export const environment = {
  production: true,
  /**
   * Your deployed backend base URL.
   * Replace with the real production URL before deploying.
   */
  apiBaseUrl: 'https://api.myapp.com/v1',

  /**
   * Always false in production — never ship mock data.
   */
  useMockBackend: false,

  appName: 'MyApp',
};
